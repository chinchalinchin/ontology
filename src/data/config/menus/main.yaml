menus:
  main:
    controller: main
    roots: 
      - id: neutral
        name: main-menu
        position:
          # ---- 360x360
          # px: 0.075
          # py: 0.15
          # ---- 480 x 480
          px: 0.175
          py: 0.275
        layout: stack
        alignment: center
        gap: 10
        children: 
          - instance: buttons
            id: label
            name: new-game
            bind: 
              selection: new
          - instance: buttons
            id: label
            name: load-game
            bind: 
              selection: load
          - instance: buttons
            id: label
            name: options-menu
            bind:
              selector: options
              selection: menu
          - instance: buttons
            id: label
            name: editor-menu
            bind:
              selector: editor
              selection: menu
  load:
    controller: load
    roots: 
      - id: neutral
        name: main-menu
        position:
          px: 0.075
          py: 0.15
        layout: stack
        alignment: center
        gap: 10
        children: 
          - instance: meters
            id: health
            name: load-bar
            bind: 
              state: context.registry