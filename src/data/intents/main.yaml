intent:
  # -------------------------- ACTIONS
  actions:
    cast:
      row: 0
      frames:
    thrust:
      row: 4
      frames:
    walk:
      row: 8
      frames:
    slash:
      row: 12
      frames:
    shoot:
      row: 16
      frames:
    die:
      row: 20
      frames:
  # -------------------------- COMMUNICATIONS
  communications: 
    - plot-00
  # -------------------------- DIRECTIONS
  directions:
    content: 
      up:
        offset: 0
      left:
        offset: 1
      down:
        offset: 2
      right:
        offset: 3
  # -------------------------- DISPOSITIONS
  dispositions:
    attack:
      transitions:
        - next: attack
          conditions:  
        - next: hunt
          conditions: 
            - sprite.disposition == 'attack'
            - sprite.target is None
            - sprite.memory.target is not None
        - next: loot
          condition: 
            - sprite.disposition == 'atack'
            - sprite.target.mutators.dead.triggered
    attract:
      transitions:
        - next: 
          condition:
    barter:
      transitions: 
        - next:
          condition:
    escape: 
      transitions:
        - next: escape
          condition: 
            - sprite.disposition == 'escape' 
            - sprite.target.disposition == 'attack'
        - next: return
          condition: sprite.
    find: 
      transitions:
        - next: loot
          condition: 
        - next: recoil
          condition:
        - next: return
          condition: 
    follow: 
      transitions: 
        - next: follow
          condition: 
        - next: hunt
          condition: 
        - next: wander
          condition: 
    idle: 
      transitions: 
        - next: idle
          condition: 
        - next: recoil
          condition: sprite.mutators.struck.triggered
        - next: wander
          condition:
    interact:
      transitions:
        - next:
          condition:
    loot:
      transitions:
        - next: return
          condition: 
    mock: 
      transitions:
        - next: mock
          condition: 
        - next: threaten
          condition: 
    recoil:
      transitions:
        - next: attack
          condition: 
        - next: escape
          condition: sprite.mutators.frightened
    return:
      transitions:
        - next: idle
          condition: 
        - next: return
          condition:
        - next: wander
          condition: 
    threaten:
      transitions:
        - next: attack
          condition: 
        - next: hunt
          condition:  
        - next: threaten
          condition: 
        - next: recoil
          condition:
    wander:
      transitions:
        - next: recoil
          condition: 