intent:
  # -------------------------- ACTIONS
  actions:
    - cast
    - thrust
    - walk
    - slash
    - shoot
    - die
  # -------------------------- COMMUNICATIONS
  communications: 
    - plot-00
  # -------------------------- DIRECTIONS
  directions:
    content: 
      - up
      - left
      - down
      - right
  # -------------------------- DISPOSITIONS
  dispositions:
    attack:
      transitions:
        - attack 
        - hunt
        - loot
        - return
    attract:
      transitions:
        - 
    barter:
      transitions: 
        - 
    escape: 
      transitions:
        - return
    find: 
      transitions:
        - loot
        - recoil
        - return
    follow: 
      transitions: 
        - follow
        - hunt
        - wander
    idle: 
      transitions: 
        - idle
        - recoil
        - wander
    interact:
      transitions:
        - 
    loot:
      transitions:
        - return
    mock: 
      transitions:
        - mock
        - threaten
    recoil:
      transitions:
        - attack
        - escape
    return:
      transitions:
        - idle
        - return
        - wander
    threaten:
      transitions:
        - attack
        - hunt 
        - threaten
        - recoil
    wander:
      transitions:
        - recoil