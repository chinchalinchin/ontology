# Registry Configuration Dump

**Board:** `{{ board_key }}`
**Timestamp:** {{ timestamp }}

---

## 1. Loaded GPU Textures

*The following physical image assets were successfully cached in VRAM:*

{% for tex in textures|sort %}
* `{{ tex }}`
{%- endfor %}

---

## 2. Mapped Frame Coordinates

*Dynamic frame keys mapped to target asset IDs and their crop coordinates:*

| Frame Key | Target Asset ID | Crop X | Crop Y | Crop W | Crop L |
| :--- | :--- | :--- | :--- | :--- | :--- |
{%- for key, data in frames|dictsort %}
| `{{ key }}` | `{{ data.item_id }}` | {{ data.crop_x }} | {{ data.crop_y }} | {{ data.crop_w }} | {{ data.crop_l }} |
{%- endfor %}