assets:
  tiles:
    frame: single
    animation: null
    behaviors: null
    state: multiplier
  cursors:
    expressions:
      frame: single
      animation: null
      behaviors:
        - <behavior>
      state: positional
    projectiles:
      frame: single
      animation: null
      behaviors:
        - <behavior>
      state: metric
  effects:
    temporary:
      frame: iterable
      animation: temporary
      behaviors:
        - <behavior>
      state: animator
    persistent:
      frame: iterable
      animation: persistent
      behaviors:
        - <behavior>
      state: animator
  objects:
    chests:
      frame: iterable
      animation: binary
      behaviors:
        - <behavior>
      state: container
    crates:
      frame: single
      animation: null
      behaviors:
        - <behavior>
      state: positional
    doors:
      frame: single
      animation: null
      behaviors:
        - <behavior>
      state: door
    gates: 
      frame: iterable
      animation: binary
      behaviors:
        - <behavior>
      state: switch
    plates:
      frame: iterable
      animation: binary
      behaviors:
        - <behavior>
      state: switch
  sheets:
    pixies:
      frame: state
      animation: state
      behaviors:
        - <behavior>
      state: pixie
    sprites:
      frame: state
      animation: state
      behaviors:
        - <behavior>
      state: sprite