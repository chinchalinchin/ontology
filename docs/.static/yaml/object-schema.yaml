objects:
  # -------------------------------------------------------
  chests:
    <key>:
      dimensions:
        length: pixel length
        width: pixel width
      hitboxes:
        - x: initial horizontal position
          y: initial vertical position
          length: pixel length
          width: pixel length
  crates:
    <key>:
      dimensions:
        length: pixel length
        width: pixel width
      hitboxes:
        - x: initial horizontal position
          y: initial vertical position
          length: pixel length
          width: pixel length
  doors:
    <key>:
      dimensions:
        length: pixel length
        width: pixel width
      hitboxes:
        - x: initial horizontal position
          y: initial vertical position
          length: pixel length
          width: pixel length
  gates:
    <key>:
      dimensions:
        length: pixel length
        width: pixel width
      hitboxes:
        - x: initial horizontal position
          y: initial vertical position
          length: pixel length
          width: pixel length
  plates:
    <key>:
      dimensions:
        length: pixel length
        width: pixel width
      hitboxes:
        - x: initial horizontal position
          y: initial vertical position
          length: pixel length
          width: pixel length