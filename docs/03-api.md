# Ontology: API

# ontology.app.asset.Asset

def get() -> (position, dimensions, frame)
    Returns 

# ontology.app.interface.player.Player

Attributes:
    - device: `controller | keyboard`

def poll() -> ()
    Returns 

# ontology.app.interface.player.Controller

def state() -> ()

# ontology.app.interface.player.Keyboard

def state() -> ()

