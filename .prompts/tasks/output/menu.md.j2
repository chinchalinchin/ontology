# Task: output(/templates/.menu-dump.md.j2)

The dump template is used to dump game state at the end of a CLI session through the command,

```bash
python main.py --dump-menus start
```

**Definitions**

- Widgets are a specialized type of Game asset.
- Widgets are Menu components. They bind to game state.
- Menus are instantiated on the fly during the game.
- Overlays are HUD components that are always rendered.

**Guidelines**

- Update the template to match the latest models.
- Ensure all whitespace in the Jinja2 template is properly handled.
- Enumerate all Widget and Menu fields. 
- Ensure Optional fields are handled appriopriately.
- Ensure there are new lines between the end of sections and the next heading.
- Ensure all attribute appear as nested markdown lists ,"-".

## CLI

```python
{{ file(properties.paths.root + '/src/cli.py') }}
```

## Latest Models

### Asset Class

```python
{{ file(properties.paths.root + '/src/app/assets/base.py') }}
```

### Widget State

```python
{{ file(properties.paths.root + '/src/app/models/state/core.py') }}
```

```python
{{ file(properties.paths.root + '/src/app/models/state/widgets.py') }}
```

### Menus

```python
{{ file(properties.paths.root + '/src/app/game/menus/core.py') }}
```

**Bindings**

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/aperture.py') }}
```

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/collection.py')}}
```

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/icon.py') }}
```

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/meter.py') }}
```

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/select.py') }}
```

```python
{{ file(properties.paths.root + '/src/app/game/menus/bindings/text.py') }}
```

## Previous Dump Template

```jinja2
{{ file(properties.paths.root + '/src/data/templates/.menu-dump.md.j2') }}
```