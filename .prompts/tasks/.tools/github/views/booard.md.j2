{% set prefixes = {
    "patch": "bug",
    "achieve": "goal",
    "implement": "phase",
    "refactor": "refactor"
} %}
{% set action = parameters.github.action %}
{% set phase = parameters.github.phase %}
{% set prefix = prefixes[action] %}
{% set path = paths.root + paths.board + 'phases/' + action + "/" + prefix + "-" + phase + ".md" %}
{# -------------------------------------------------  #}
{% if views.github.board %}
### View: {{ project.title | capitalize }} Task Board

- **Current Phase**: {{ prefix | upper }} {{ phase | upper }}
- **Current Phase Action**: {{ action | upper }}

{{ file(paths.root + paths.board + 'index.md') }}

{{ file(path) }}
{% endif %}